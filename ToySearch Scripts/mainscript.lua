local pointhandler = require(script.Parent.pointhandler)
local addpoints = pointhandler.addpoints
local ds = game:GetService("DataStoreService")
--[[
local points;
--]]

game.Players.PlayerAdded:Connect(function(p)
    
end)
